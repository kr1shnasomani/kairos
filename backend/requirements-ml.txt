# ML / AI requirements — heavy dependencies for perception engine and NER
# Install separately: pip install -r requirements-ml.txt
# These are NOT required to run the API stub — only needed for Layer 3 (perception engine)

# --- OCR ---
paddlepaddle==2.6.1
paddleocr==2.8.1

# --- NLP / NER ---
transformers==4.41.2
tokenizers==0.19.1
sentencepiece==0.2.0
datasets==2.20.0
accelerate==0.31.0

# --- LLM / Inference ---
# Ollama is accessed via REST — no pip install needed
# NVIDIA NIM is OpenAI-compatible — use openai package
openai==1.35.3

# --- Embeddings ---
sentence-transformers==3.0.1

# --- Voice Transcription (Whisper) ---
openai-whisper==20231117

# --- Computer Vision (YOLOv9 drawing parser) ---
torch==2.3.1
torchvision==0.18.1
ultralytics==8.2.40
layoutparser==0.3.4
Pillow==10.3.0
opencv-python-headless==4.9.0.80

# --- Scientific Computing ---
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.1
pandas==2.2.2
